- fix the member implementation and integration with main.pony

- Update the 3d topology to have 6 neighbors instead of 4
- update the imperfect 3d topology to have 6 + 1 imperfect node instead of 4

- bonus section - add timeout to a few random nodes to see how network converges, - infect the rumor after a certain number of messages have been sent to see the error.